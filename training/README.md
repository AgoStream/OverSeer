# overseer-training (Component A)

Offline pipeline that labels hardware contention regimes and trains the interference model.
Exports model artifacts consumed by Component B (`pkg/regime`).

## Setup

```bash
pip install -e ".[dev]"
```

## Run tests

```bash
pytest
```
